name: Mart daily scrape

on:
  schedule:
    # 5:00 AM Pakistan time (UTC+5) == 00:00 UTC
    - cron: '0 0 * * *'
  # Manual run from the Actions tab -- useful for testing and re-runs.
  workflow_dispatch:

# Only one scrape at a time -- if a long run overlaps the next schedule
# we just skip the newer one rather than racing two browsers.
concurrency:
  group: mart-scrape
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write  # needed so we can git push the new JSON back

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # (Cloudflare WARP step removed -- WARP DNS couldn't resolve the
      # target host. Re-introduce it later with a DNS override if we still
      # need to mask the runner IP.)

      - name: Show outbound IP (for debugging)
        run: |
          echo "outbound IPv4: $(curl -s --max-time 10 https://api.ipify.org || echo unknown)"
          echo "outbound geo : $(curl -s --max-time 10 https://ipinfo.io | head -c 400 || echo unknown)"

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install 'patchright>=1.49.0' 'playwright-stealth==1.0.6'

      - name: Install Chromium for Patchright
        run: |
          # Patchright bundles its own patched Chromium download command.
          python -m patchright install --with-deps chromium

      - name: Run scraper
        env:
          MART_EMAIL:    ${{ secrets.MART_EMAIL }}
          MART_PASSWORD: ${{ secrets.MART_PASSWORD }}
          MART_HOST:     ${{ secrets.MART_HOST }}
          MART_COOKIES:  ${{ secrets.MART_COOKIES }}
          MART_PROXY:    ${{ secrets.MART_PROXY }}
        run: python Scraper/Scraper.py

      - name: Upload debug snapshot on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-debug
          path: Scraper/debug/
          if-no-files-found: ignore

      - name: Commit & push new data file
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Scraper/data/
          if git diff --cached --quiet; then
            echo "No new data to commit."
          else
            git commit -m "data: scrape $(TZ=Asia/Karachi date +%F) [skip ci]"
            git push
          fi
